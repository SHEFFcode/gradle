import java.text.*


defaultTasks 'hello'

task ('hello').doLast {
  println "Hello world"
}

task ('printDate').doLast {
  SimpleDateFormat sdf = new SimpleDateFormat("yyy-MM-dd'T'HH:mm:ss'Z'")
  sdf.setTimeZone(TimeZone.getTimeZone("UTC"))
  println "Hello world " + sdf.format(new Date())
}

[1, 2, 3].each {
  println it
}

class Person {
  String name = "Fred"

  Closure nameSayer = {
    println name
  }
}

def p1 = new Person()

p1.nameSayer() // Closures keep a refence to the place they've been defined.  They keep reference to the context rather than the values.

logger.info "Hello!"

apply {
  println "Hello again!!!"
}